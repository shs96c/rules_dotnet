load(
    "@rules_dotnet//dotnet:defs.bzl",
    "csharp_library",
)

csharp_library(
    name = "ManifestLoader",  # Do not change the name on this target without updating the runtimeconfig generation
    srcs = [
        "BazelManifest.cs",
        "StartupHook.cs",
    ],
    private_deps = [
        "@rules_dotnet_nuget_packages//microsoft.netcore.app.ref",
    ],
    target_frameworks = ["net6.0"],
    visibility = ["//visibility:public"],
    deps = [
    ],
)
